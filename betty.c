#include "shell.h"

/**
 * print_number - print the number from 0 - 9
 *
 * Return: void
 */

void print_number(void)
{
	int i;

	for (int = 0; i < 10; i++)

		printf("%d", i);
	printf("\n");
}

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	print_number();
	return (0);
}
